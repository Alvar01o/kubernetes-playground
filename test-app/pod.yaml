apiVersion: v1
kind: Pod
metadata:
  name: test-app
  labels:
    app: test-app
spec:
  containers:
    - name: test-app
      image: test-app:latest
      # Never pull from a registry — use the image loaded into Minikube locally
      imagePullPolicy: Never
      ports:
        - containerPort: 3000
